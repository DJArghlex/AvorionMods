-- print ("beginning faction indexing! this can consume a LOT of RAM and disk I/O and take a long time. be patient, and disable the checkserver.py script."); local galaxy = Galaxy(); for i=1,2005000 do if galaxy:aiFactionExists(i) then print("#" .. i .. ": " .. Faction(i).name .. " / NPC") elseif galaxy:allianceFactionExists(i) then print("#" .. i .. ": " .. Faction(i).name .. " / Alliance") elseif galaxy:playerFactionExists(i) then print("#" .. i .. ": " .. Faction(i).name .. " / Player") end end; print ("faction indexing complete!")